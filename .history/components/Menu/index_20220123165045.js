import React from 'react';
import styled from '@emotion/styled';

const Menu = styled.div`


`

const Menu = () => {
    return (
        <div>
            <Menu></Menu>
        </div>
    );
};

export default Menu;
