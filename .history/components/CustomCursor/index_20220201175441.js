import React, { useEffect } from 'react';

const CustomCursor = () => {
    const customCursor = React.useRef(null)
    useEffect(() => {
        if(typeof window === 'inde')
    })
    return (
        <div>

        </div>
    )
};

export default CustomCursor;
